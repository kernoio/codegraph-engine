/**
 * Always-on Kerno built-in plugins — thin re-export of in-repo plugins under
 * `src/plugins/`. Stock upstream resolvers stay in FRAMEWORK_RESOLVERS.
 */

export { getBuiltInPlugins } from '../plugins';
